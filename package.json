{
  "name": "json-question-processor",
  "version": "1.0.0",
  "description": "A web application to process JSON files and export them to Word documents",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "json",
    "questions",
    "processor",
    "web-app"
  ],
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "docx": "^9.5.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
